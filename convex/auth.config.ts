// Auth config is no longer needed with NextAuth.js
// Authentication is now handled directly through NextAuth.js
// and Convex queries/mutations are called without auth middleware

const authConfig = {
  providers: [],
};

export default authConfig;
